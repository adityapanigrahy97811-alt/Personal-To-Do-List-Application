# Personal-To-Do-List-Application
A simple Python-based To-Do List Application that lets users create, view, edit, and delete tasks. It uses file handling (tasks.txt) to store data instead of a database. Features a command-line interface focusing on CRUD operations, user interaction, and logical code organization.
